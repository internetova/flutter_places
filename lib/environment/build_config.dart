/// описывает окружение в котором запускаем приложение
/// [envDebugAppBarLabel] для дебажной сборки надпись в аппбаре
class BuildConfig {
  final String? envDebugAppBarLabel;

  BuildConfig({
    this.envDebugAppBarLabel,
  });
}
